import { Slider } from '@/components/ui/slider';

const SliderComponents = () => {
  return <Slider className="text-text" defaultValue={[33]} max={100} step={1} />;
};

export default SliderComponents;
